# Dependencies and `npm pack` output
node_modules/
*.tgz

# Runtime state: state, sessions, stats, prefs, locks, pid and quit files.
# They normally live in $HOME; ignored here in case HOME or CODE_CRUMB_STATE
# points into the checkout.
.code-crumb*

# Local-only working folders (docs/ holds untracked design specs)
.claude
docs/
TRASH/

# Windows shell debris: `> nul` under bash creates a file named nul, and
# tasklist stderr once landed in a file named _ul
nul
_ul

# OS noise
.DS_Store
Thumbs.db
.Related Images
